// src/lib/keymgmt.js

module.exports = {
    // generate a new key
    // returns { cmkId, encryptedDataKey, dataKey }
    generateKey: function() {
        
    },
    // decrypt data key given cmkId and encyptedDataKey
    // returns { dataKey }
    decryptKey: function(cmkId, encyptedDataKey) {
        
    }
}